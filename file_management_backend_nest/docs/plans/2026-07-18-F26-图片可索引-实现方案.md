# F-26 图片可索引 — 实现方案

> 日期：2026-07-18  
> 状态：**已完成（S18a）**  
> PRD：[2026-07-08-ai-features-prd.md](./2026-07-08-ai-features-prd.md) § F-26（**v1.18 交付回写**）  
> 修订：2026-07-18 — 带写落地 + OCR 划词面板 + 三栏内双击大图

## 目标

png / jpg / jpeg / webp / gif → OCR（硅基 `DeepSeek-OCR`）→ 复用现有 chunk / embed / 摘要 / RAG 流水线；图片预览挂 `DocumentAiPanel`。

索引 `ready` 后，图片预览增加 **OCR 文本中间栏**，在此划词并复用右侧划词问答；**不改变** Word / TXT / PDF 的左右两栏布局。

## 后端

| 步骤 | 文件 | 要点 | 状态 |
|------|------|------|------|
| 1 | `src/files/ai/vision/vision.provider.ts` | `extractTextFromImage` → 硅基 `chat/completions` + `Free OCR.` | ✅ |
| 2 | `vision.provider.spec.ts` | mock `fetch` | ✅ |
| 3 | `chunk/text-extractor.ts` | 图片扩展名+MIME；OCR 分支 | ✅ |
| 4 | `files-ai-index.service.ts` | 400 文案含可 OCR 图片 | ✅ |
| 5 | e2e `files-ai-index` | png → pending；mp4 → 400 | ✅ |
| 6 | `GET .../ai/extracted-text` | `joinOverlappingChunkContents` 拼近似全文 | ✅ |

## 环境变量

```
AI_VISION_BASE_URL=https://api.siliconflow.cn/v1
AI_VISION_API_KEY=...   # 可与 Embedding 同一把硅基 Key
AI_VISION_MODEL=deepseek-ai/DeepSeek-OCR
```

（`.env.example` 已收录。）

## 前端

| 步骤 | 文件 | 要点 | 状态 |
|------|------|------|------|
| 1 | `ImageDocumentPreviewDialog` | 三栏：图 \| OCR \| AI；中间划词 → `selectedText` | ✅ |
| 2 | `index.vue` | 双击图片打开三栏 Dialog | ✅ |
| 3 | 同 Dialog 内双击图片 | 打开 `CustomImageViewer` 大图轮播（同目录画廊） | ✅ |
| 4 | i18n | `preview.imageTitle`、`preview.ocrPanel*`、`imageDblclickLightbox` | ✅ |
| 5 | **不改** | PDF / TXT / Office 预览仍左右两栏 | ✅ |

## 验收对照

1. ✅ 带文字 png/jpg → 建索引 → `ready`；中间栏 OCR；划词 / RAG 可用  
2. ✅ Worker + Redis 需启动；空白/失败有友好错误  
3. ✅ Word/TXT/PDF 仍两栏  
4. ✅ 单测 vision / text-extractor；e2e 图片入队  

## 不做（本特性）

扫描 PDF（F-30）、截图解题 VL（F-27）、bmp 索引、**图片像素框选 / OCR 坐标映射**。

## 交付记录（2026-07-18）

- 核心 OCR：`extractTextFromImage`（多模型路由：`AI_VISION_*` vs 文本 `AI_*`）  
- 索引复用：`document-index.processor` 未改结构  
- 前端：`ImageDocumentPreviewDialog` + `GET /files/:id/ai/extracted-text`  
- 带写流程：guided-step-coaching 分步落地；先预览实现再回滚带写  
- **PRD 回写：v1.18**；其后已交付 **F-27**；当前下一批 **F-29 → F-30**

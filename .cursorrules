# 文件管理项目规则 (File Management Project Rules)

## 技术栈 (Tech Stack)
- **前端**: Vue 3 + TypeScript + Vite
- **后端**: Express + TypeScript
- **数据库**: MySQL + Prisma ORM
- **包管理器**: pnpm (必须使用)

## 开发工作流 (Development Workflow)
1. **依赖管理**: 必须使用 `pnpm` 安装或更新包。
   - 示例: `pnpm add <package>` 或 `pnpm install`
2. **API 文档**:
   - 创建或修改后端 API 时，**必须**同步更新 Swagger 文档。
   - Swagger 配置位于 `src/config/swagger.config.ts` 和各个路由文件中。
3. **数据库变更**:
   - 严禁在生产环境数据上直接使用 `prisma migrate dev`。
   - 必须参考 `database-migration` skill 进行操作。
   - 任何 Schema 变更前**必须备份数据**。

## 编码规范 (Coding Standards)
- **语言**: 所有新代码使用 TypeScript。
- **注释**: 必须使用**中文**编写注释。
- **文档**: 所有文档（包括 README、Skill、API 文档）都必须使用**中文**描述。
- **响应**: 回答用户问题时使用**中文**。

## 系统人设 (System Personality)
- 你是一位乐于助人、专业的全栈工程师专家。
- 主动提供最佳实践建议。

# 未完成需求清单

> **对照文档**：[REQUIREMENTS.md](./REQUIREMENTS.md)（建议同步阅读 **[实现状态总览](./REQUIREMENTS.md#实现状态总览)**）  
> **整理日期**：2026-04-18  
> **说明**：下列条目为相对需求文档**尚未完成**或**仅部分满足**的内容；已实现部分不在此重复罗列。

---

## 1. 用户管理

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §1(6) | 新消息浏览器通知 | **未实现** | 使用 Web **Notification API** 在收到新消息时提示（需权限与前台/后台策略）。 |
| §1(8) | 密码 SHA256、重置、修改 | **部分** | SHA256 与管理员/忘记密码路径的重置已有；仍缺 **用户自助「修改密码」且须校验原密码**；需求中「重置为**统一初始密码**」与当前「管理员指定新密码」不一致，若需严格一致需产品确认。 |

**说明**：§1(1) 普通 / VIP / 管理员角色、配额（1GB / 2GB）、VIP 申请与管理员审核（含会话内同意/拒绝）已在本版实现，详见 [REQUIREMENTS.md](./REQUIREMENTS.md) 实现状态总览与「VIP 升级与审核流程」。§1(2)(3)(4)(5)(7)(9) 等已在当前版本中实现或基本满足，不列入上表。

---

## 2. 文件管理

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §2(17) | 大文件分页预览 | **未实现 / 弱** | 超大文本或超大文件的 **分页 / 分段加载预览**（与音频、视频播放进度记忆无关；后者已验收）。 |

**说明**：§2(1-1) 压缩包在线解压（ZIP、VIP/管理员）、§2(5) 上传/下载历史、§2(8) 搜索、§2(9) 排序、§2(10) 批量下载（ZIP）、批量删除、批量移动、§2(13) 标签、§2(16-1) 网格视频悬停预览、**音频与视频播放进度记忆**等**已在当前版本实现并验收**，不列入上表。

---

## 3. 文件分享（链接分享）

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §3(3) | 分享权限（仅查看 / 可下载 / 可编辑） | **部分** | 创建分享时目前固定为可下载能力向；需 **创建时可选权限**，并在访客 **列表/预览/下载/编辑（若产品定义）** 全链路按 `SharePermission` 校验。**可编辑**是否与网盘内协作一致须产品定界。 |
| §3(4) | 分享访问记录（分享者可见） | **部分** | 访客访问已在后端写入 `ShareAccessLog`（时间、IP、UA、view/download）；缺 **分享者侧**「我的分享 / 访问记录」**列表 API + 前端**（可按分享聚合展示）。 |
| — | 访问人数上限 | **部分** | `max_visitors` 已存储；**访客端达到上限后拒绝访问**的统计与校验待实现（若保留该能力）。 |

**说明**：§3(1) 公开链接 / 私密（提取码）、分享页与 §3(2) 有效期（含过期校验与定时清理过期分享）**已在当前版本实现**，不列入上表。好友会话内传文件与「链接分享」不是同一模块。

---

## 4. 权限管理

**说明**：原 §4(2)「管理员手动清理无引用物理文件」**已从产品需求中移除**，无引用文件仅依赖 **§2(6) 24h 定时任务**自动物理删除；详见 [REQUIREMENTS.md](./REQUIREMENTS.md) §4 与实现状态总览。本节当前**无**待开发条目。

---

## 5. 安全性

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §5(2) | Token 黑名单 | **部分** | 除 Refresh 撤销外，是否实现 **Access Token 黑名单** 或等效强失效机制。 |
| §5(4) | 异常登录 / 异地提醒 | **未实现** | IP/地域策略、提醒渠道（站内信、邮件等）。 |
| §5(5) | 敏感文件加密存储（可选） | **未实现** | 可选能力，可排期。 |
| §5(7) | 会话管理、远程登出设备 | **未实现** | 展示 Refresh/会话列表，支持按设备撤销。 |

**说明**：§5(1)(3)(6) 双 Token、登录日志、操作日志等已具备基础能力。

---

## 6. 管理员功能

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §6(3) | 违规文件检测与处理 | **未实现** | 审核队列、标记、删除或禁用分享等流程。 |

**说明**：§6(1) 用户列表与禁用/启用、§6(2) 存储统计等已实现；若需与需求逐字对齐可再验收文案与权限。

---

## 其他需求（国际化 / 主题）

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| — | 简中 / 繁中 / 英文 + 持久化 | **已完成** | 无 |
| — | 浅色 / 深色主题 + 持久化 | **已完成** | 无 |

---

## 建议优先级（参考）

1. **高**：§1(8) 用户修改密码（校验原密码）；§3 **权限三档**与 **分享者访问记录** 补全。  
2. **中**：§2(17) 大文件分页预览；§5(7) 会话管理；§3 访问人数上限（若产品保留）。  
3. **低 / 可选**：§5(4)(5)、§6(3)、§5(2) 黑名单增强。

---

## 与进度报告的关系

详细历史对照与统计见 [PROGRESS_REPORT.md](./PROGRESS_REPORT.md)。本文档仅维护**未完成 / 部分完成**清单，便于排期与验收；主需求仍以 **REQUIREMENTS.md** 为准。

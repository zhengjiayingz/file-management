# 未完成需求清单

> **对照文档**：[REQUIREMENTS.md](./REQUIREMENTS.md)（建议同步阅读 **[实现状态总览](./REQUIREMENTS.md#实现状态总览)**）  
> **整理日期**：2026-04-18  
> **说明**：下列条目为相对需求文档**尚未完成**或**仅部分满足**的内容；已实现部分不在此重复罗列。

---

## 1. 用户管理

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §1(1) | 普通 / VIP / 管理员与配额 | **部分** | 缺少 **VIP 角色**、**注册后升级 VIP** 的流程；VIP 2GB 配额策略未与角色联动。 |
| §1(6) | 新消息浏览器通知 | **未实现** | 使用 Web **Notification API** 在收到新消息时提示（需权限与前台/后台策略）。 |
| §1(8) | 密码 SHA256、重置、修改 | **部分** | SHA256 与管理员/忘记密码路径的重置已有；仍缺 **用户自助「修改密码」且须校验原密码**；需求中「重置为**统一初始密码**」与当前「管理员指定新密码」不一致，若需严格一致需产品确认。 |

**说明**：§1(2)(3)(4)(5)(7)(9) 等已在当前版本中实现或基本满足，不列入上表。

---

## 2. 文件管理

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §2(1-1) | 压缩包在线解压（ZIP、VIP/管理员） | **需核对** | 请对照需求中的列表树、重名预检、冲突策略、非 ZIP 提示等，与现有 `archive` 接口及前端弹窗逐项验收；若有缺口则补全。 |
| §2(17) | 大文件分页预览、视频进度记忆 | **未实现 / 弱** | 大文本/大文件 **分页预览**；视频播放器 **播放进度本地或服务端记忆**。 |

**说明**：§2(5) 上传/下载历史、§2(8) 搜索（含「上传时间」筛选与文案）、§2(9) 排序（含按类型）、§2(10) 批量下载（ZIP，含文件夹递归）、批量删除（`POST /api/files/batch/delete`）、批量移动（`POST /api/files/batch/move`）、§2(13) 标签、§2(16-1) 网格视频悬停预览等**已在当前版本实现**，不列入上表。

---

## 3. 文件分享（链接分享）

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §3(1) | 公开/私密链接与提取码 | **未实现** | 生成分享链接、提取码、访问页。 |
| §3(2) | 有效期（1 天 / 7 天 / 30 天 / 永久） | **未实现** | 与分享模型、定时失效或校验逻辑联动。 |
| §3(3) | 分享权限（仅查看 / 可下载 / 可编辑） | **未实现** | 与分享资源、访客操作校验一致。 |
| §3(4) | 分享访问记录 | **未实现** | 谁、何时、何种操作；与 `ShareAccessLog` 或等价表对接展示。 |

**说明**：好友会话内传文件与需求中的「链接分享」不是同一模块。

---

## 4. 权限管理

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §4(2) | 管理员手动清理无引用物理文件 | **未实现 / 弱** | 在仅依赖 **24h 定时任务** 之外，增加管理员 **手动触发清理** 或「待删除队列」管理界面（需求原文要求）。 |

---

## 5. 安全性

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §5(2) | Token 黑名单 | **部分** | 除 Refresh 撤销外，是否实现 **Access Token 黑名单** 或等效强失效机制。 |
| §5(4) | 异常登录 / 异地提醒 | **未实现** | IP/地域策略、提醒渠道（站内信、邮件等）。 |
| §5(5) | 敏感文件加密存储（可选） | **未实现** | 可选能力，可排期。 |
| §5(7) | 会话管理、远程登出设备 | **未实现** | 展示 Refresh/会话列表，支持按设备撤销。 |

**说明**：§5(1)(3)(6) 双 Token、登录日志、操作日志等已具备基础能力。

---

## 6. 管理员功能

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| §6(3) | 违规文件检测与处理 | **未实现** | 审核队列、标记、删除或禁用分享等流程。 |

**说明**：§6(1) 用户列表与禁用/启用、§6(2) 存储统计等已实现；若需与需求逐字对齐可再验收文案与权限。

---

## 其他需求（国际化 / 主题）

| 编号 | 需求摘要 | 状态 | 待完成内容 |
|------|----------|------|------------|
| — | 简中 / 繁中 / 英文 + 持久化 | **已完成** | 无 |
| — | 浅色 / 深色主题 + 持久化 | **已完成** | 无 |

---

## 建议优先级（参考）

1. **高**：§3 整段链接分享；§1(1) VIP 与配额联动；§1(8) 用户修改密码。  
2. **中**：§2(17) 大文件分页预览与视频进度记忆；§5(7) 会话管理；§2(1-1) 在线解压场景验收。  
3. **低 / 可选**：§5(4)(5)、§6(3)、§4(2) 手动清理、§5(2) 黑名单增强。

---

## 与进度报告的关系

详细历史对照与统计见 [PROGRESS_REPORT.md](./PROGRESS_REPORT.md)。本文档仅维护**未完成 / 部分完成**清单，便于排期与验收；主需求仍以 **REQUIREMENTS.md** 为准。

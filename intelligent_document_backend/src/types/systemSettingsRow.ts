import type { SystemSettings } from '@prisma/client';

/**
 * 与 `schema.prisma` 中 `SystemSettings` 及 `@prisma/client` 生成类型一致（单一事实来源）。
 * 修改模型后请执行 `npx prisma generate`。
 */
export type SystemSettingsRow = SystemSettings;
